[advisories]
ignore = [
    # Transitive dependency in kube-runtime 0.87.x (MSRV-constrained stack).
    "RUSTSEC-2025-0012", # backoff (unmaintained)
    "RUSTSEC-2024-0388", # derivative (unmaintained)
    "RUSTSEC-2024-0384", # instant (unmaintained)
    # Transitive dependency in sled 0.34.x.
    "RUSTSEC-2025-0057", # fxhash (unmaintained)
    # Transitive dependency in indicatif 0.17.x.
    "RUSTSEC-2025-0119", # number_prefix (unmaintained)
    # Transitive dependency in hyper-rustls 0.24.x.
    "RUSTSEC-2025-0134", # rustls-pemfile (unmaintained)
]
